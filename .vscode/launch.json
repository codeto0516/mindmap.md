{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "拡張を起動 (Extension Development Host)",
      "type": "extensionHost",
      "request": "launch",
      "args": ["${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "preLaunchTask": "build:extension-and-webview"
    }
  ]
}
